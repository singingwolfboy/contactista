import React from 'react';
import ReactDOM from 'react-dom';

import QueryWrapper from './QueryWrapper';

const mountNode = document.getElementById('root');

ReactDOM.render(<QueryWrapper />, mountNode);
